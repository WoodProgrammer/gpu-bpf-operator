---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cudaebpfpolicies.gpu.obs.gpu
spec:
  group: gpu.obs.gpu
  names:
    kind: CudaEBPFPolicy
    listKind: CudaEBPFPolicyList
    plural: cudaebpfpolicies
    singular: cudaebpfpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CudaEBPFPolicy is the Schema for the cudaebpfpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CudaEBPFPolicySpec defines the desired state of CudaEBPFPolicy.
            properties:
              functions:
                items:
                  properties:
                    args:
                      items:
                        properties:
                          index:
                            type: integer
                          name:
                            type: string
                        required:
                        - index
                        - name
                        type: object
                      type: array
                    kind:
                      type: string
                    name:
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              libPath:
                type: string
              mode:
                type: string
              output:
                type: string
              processRegex:
                type: string
            required:
            - functions
            - libPath
            - mode
            type: object
          status:
            description: CudaEBPFPolicyStatus defines the observed state of CudaEBPFPolicy.
            properties:
              observedHash:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
