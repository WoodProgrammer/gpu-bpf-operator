---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: probetargetbindings.gpu.obs.gpu
spec:
  group: gpu.obs.gpu
  names:
    kind: ProbeTargetBinding
    listKind: ProbeTargetBindingList
    plural: probetargetbindings
    singular: probetargetbinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProbeTargetBinding is the Schema for the probetargetbindings
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProbeTargetBindingSpec defines the desired state of ProbeTargetBinding.
            properties:
              canaryPercent:
                type: integer
              maxUnavailable:
                type: integer
              nodeSelector:
                additionalProperties:
                  type: string
                type: object
              policyRef:
                type: string
            required:
            - policyRef
            type: object
          status:
            properties:
              appliedHash:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
